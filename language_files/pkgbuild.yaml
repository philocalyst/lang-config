language:
    - comment-token: '#'
      file-types:
        - glob: PKGBUILD
      grammar: bash
      language-servers:
        - termux-language-server
        - except-features:
            - diagnostics
          name: bash-language-server
      name: pkgbuild
      scope: source.bash
